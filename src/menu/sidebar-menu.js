import {
    Home,
    AppWindow,
    Contact,
    MoveUpRight,
    ArrowRight,
} from "lucide-react";

/** @type {SidebarMenuItem[]} */
const sidebarMenu = [{ title: "Book List", icon: Home, path: "/" }];

export default sidebarMenu;
